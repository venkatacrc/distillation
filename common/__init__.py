"""Shared utilities used across the distillation labs.

Import this package after installing it in editable mode once:

    pip install -e .

from the repo root (see the top-level README's Quickstart section).
"""
